/* function entrar() {

    var area = document.getElementById('login');
    var text = prompt('Digite seu nome');

    if (text == '' || text == null) {
        alert('Digite seu nome novamente');

        area.innerHTML = 'Bem vindo... ';
    } else {
        area.innerHTML = 'Bem vindo ' + text;
    }
}

function entrar2(nome) {

    var area = document.getElementById('login2');
    var text = prompt('Digite seu sobrenome');
    area.innerHTML = nome + " " + text;



}

function idade1(age1) {

    var area = document.getElementById('idade');
    var text = prompt('Digite Sua idade');
    area.innerHTML = age1 + " " + text;

} */